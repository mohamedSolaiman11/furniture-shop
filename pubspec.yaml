name: furniture_store
description: "A premium furniture showroom website built with Flutter."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.1
  google_fonts: ^6.1.0
  fl_chart: ^0.66.2
  uuid: ^4.3.3
  intl: ^0.20.0
  url_launcher: ^6.3.2
  shared_preferences: ^2.2.2
  supabase_flutter: ^2.6.0
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
